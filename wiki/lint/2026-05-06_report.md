# Wiki lint report — 2026-05-06 (L-W2)

End-of-Week-2 lint pass. Findings only — no fixes applied. Triage by the
human; remediations belong in follow-up tasks.

## Scope

- All files under `wiki/` and `drafts/`.
- Checks per `docs/lint-protocol.md`: orphans, missing pages,
  contradictions, stale status, index drift, citation gaps, dead links.
- Cross-checked against `TASKS.md` Completed entries and
  `wiki/lint/2026-04-21_sync-report.md` (S9, prior pass).

## Inventory snapshot

- 4 algorithm pages, 13 concept pages, 17 source pages, 1 lint report,
  `index.md`, `log.md`, 2 draft chapters.
- Total wikilinks parsed: 71 unique targets (35 page-level, 12 anchored,
  plus 1 template line in `index.md`, 1 malformed link, 3 deliberate
  forward references).

---

## High

### H1 — Dashboard arithmetic drift in `TASKS.md`

Actual `[x]/[?]/[~]/[ ]/[!]` counts vs declared dashboard:

| State | Dashboard (post L-W2 flip) | Actual |
| --- | --- | --- |
| Completed `[x]` | 23 | **24** |
| In Review `[?]` | 1 | **0** |
| In Progress `[~]` | 1 | 1 |
| Not Started `[ ]` | 57 | 57 |
| Blocked `[!]` | 0 | 0 |
| Sum | 82 | 82 |

The pre-L-W2 dashboard (`Completed: 23 · In Review: 1 · In Progress: 0 ·
Not Started: 58`) inherited an off-by-one from earlier flips (likely the
T13 merge): one Completed task was double-counted as `[?]` In Review. The
mechanical L-W2 flip preserved this drift rather than silently
correcting it. No actual `[?]` row exists anywhere in `TASKS.md`.

Recommended fix: dashboard line should read
`Completed: 24 · In Review: 0 · In Progress: 1 · Not Started: 57 ·
Blocked: 0`. The Backlog item under `## Backlog` already calls out
"Watch for re-drift during future flips" — this is that re-drift.

Severity: **High** because it affects the source of truth on project
progress; the human reviews this on every pickup.

---

## Medium

### M1 — Missing pages referenced by 3+ pages

Three concept pages are referenced from multiple wiki pages but do not
yet exist on disk. All three are scheduled forward references with
explicit "pending TXX" annotations, so they are not lint errors per se —
but they meet the lint-protocol §2 criterion (≥3 referrers, no page) and
are reported here for completeness.

| Target | Referrers | Owning task | Status |
| --- | --- | --- | --- |
| `concepts/adversary-model` | `algorithms/{pbft,pos,avalanche,dag-based}.md` (×4), `concepts/{fault-model,evaluation-metrics,research-questions}.md` (research-questions ×2 in body), `sources/...gervais-pow-security-2016.md` | T18 | `[ ]` Not Started (Week 3) |
| `concepts/experiment-matrix` | `concepts/{research-questions,evaluation-metrics}.md`, `sources/...gervais-pow-security-2016.md` | T19 | `[ ]` Not Started (Week 3) |
| `concepts/message-types` | `sources/...yin-hotstuff-2019.md` | T16 | `[ ]` Not Started (Week 3) |

Side observation: the S5 ingest log entry (`wiki/log.md:34`) claimed
these forward links "will resolve by L-W2." That was incorrect — they
resolve under W3 task content (T16, T18, T19), not under L-W2 itself.
L-W2 is a lint pass; it does not author content. No edit is needed
because `wiki/log.md` is append-only history; flagging here so future
agents do not re-derive the wrong expectation from the S5 log.

### M2 — Stale `TODO(cite)` markers superseded by S6/S9

Three `TODO(cite)` markers in foundation pages ask for bibliographic
work that has since been completed. The cited works now have dedicated
`wiki/sources/` pages, so the markers can be resolved by replacing them
with a `[[sources/...]]` pointer or by deleting them outright (the
`[[concepts/annotated-bibliography]]` page is the canonical resolver).

| Page:line | Marker target | Now resolvable via |
| --- | --- | --- |
| `wiki/concepts/byzantine-generals.md:36` | Lamport, Shostak, Pease 1982 (DOI/pagination) | `[[sources/2026-04-21_lamport-shostak-pease-bgp-1982]]` (entry `[1]`) |
| `wiki/concepts/flp-impossibility.md:38` | Fischer, Lynch, Paterson 1985 (full bib record) | `[[sources/2026-04-21_flp-impossibility-1985]]` (entry `[2]`) |
| `wiki/concepts/problem-statement.md:167` | "Link bracketed refs to source pages once S6 lands" | S6 + S9 have landed; all `[1]`–`[17]` resolve via `[[concepts/annotated-bibliography]]` |

Severity: **Medium** because these are conventional `TODO(cite)`
markers per `docs/wiki-spec.md` §"Citations in drafts" and they have
become stale. Not high because no Writer task is currently blocked on
them (drafts cite the bibliography page, which is itself complete).

---

## Low

### L1 — Genuine outstanding citation gap (CAP theorem)

`wiki/concepts/cap-theorem.md:33` carries `TODO(cite) — add Gilbert &
Lynch's 2002 proof of the conjecture.` This is **not** stale: the
Gilbert–Lynch 2002 paper is not in the consolidated `[1]`–`[17]`
bibliography and has no `wiki/sources/` page. The CAP page currently
cites only Brewer's 2000 PODC keynote.

Resolution options (defer to human):
1. Promote Gilbert–Lynch 2002 into the bibliography as `[18]` and
   create a source page (one Researcher task).
2. Leave as `TODO(cite)` until a Writer task surfaces a CAP claim that
   needs the formal proof (current drafts do not).

### L2 — Malformed wikilink (directory target)

`wiki/concepts/annotated-bibliography.md:170` contains
`[[sources/|source pages]]`. This uses Obsidian wikilink alias syntax
to point at a directory — wikilinks resolve to pages, not directories,
so the link is broken on render and on the retrieval graph (it has no
target file).

Resolution: rewrite as plain prose ("dedicated source pages under
`wiki/sources/`") or as a Markdown link if a directory listing is
desired.

### L3 — Soft-orphan source pages (only index + bibliography inbound)

Per the `<300 wiki pages` retrieval scheme in `docs/retrieval.md`, the
index counts as inbound coverage, so these are **not** strict orphans.
But they receive no inbound links from algorithm or concept pages where
their content is contextually relevant — meaning a Writer following
wikilinks outward from a topic page will not encounter them.

| Source page | Inbound (incl. index, bibliography) | Suggested upstream link |
| --- | --- | --- |
| `sources/...cachin-vukolic-blockchain-wild-2017` (`[16]`) | 2 | `wiki/concepts/consensus-overview` (qualitative review of permissioned BFT motivates simulator-based study) |
| `sources/...flp-impossibility-1985` (`[2]`) | 2 | `wiki/concepts/flp-impossibility#source` (page already cites the work, but does not wikilink the dedicated source page) |
| `sources/...gervais-pow-security-2016` (`[17]`) | 2 | already linked from `wiki/concepts/problem-statement:165`; consider a second link from `wiki/concepts/evaluation-metrics` (methodological precedent for instrumentation contract) |
| `sources/...xiao-survey-2020` (`[15]`) | 2 | `wiki/concepts/consensus-families` or `wiki/concepts/evaluation-metrics#reported-ranges-in-the-literature` (survey supplies the cross-family ranges) |

Severity: **Low** because all four are reachable via the bibliography
hub; none would be missed by a Researcher doing a standard index→topic
walk.

### L4 — `wiki/log.md` accuracy nit (informational, no action)

`wiki/log.md:34` (S5 entry) and `wiki/log.md:44` (S7 entry) state that
forward dead links to `concepts/adversary-model` and
`concepts/experiment-matrix` "will resolve by L-W2." They actually
resolve under T16/T18/T19 (Week 3 modeling tasks). The log is
append-only history; no edit is recommended. Surfaced so future agents
do not act on the wrong expectation.

---

## Checks that came back clean

- **Orphans (strict)**: none. Every wiki page has ≥2 inbound links.
- **Index drift**: clean. All 4 algorithm + 13 concept + 17 source + 1
  lint + 2 draft files appear in `wiki/index.md`, and every index entry
  resolves to a file on disk.
- **Stale status (artifact existence)**: clean. Every `[x]` Completed
  task in `TASKS.md` has its declared artifact present on disk,
  including the post-S9 fix-up that added `wiki/concepts/consensus-overview.md`
  for T1/T1.1 and the T13 update on `wiki/concepts/problem-statement.md`.
- **Citation gaps in drafts**: clean. Zero `TODO(cite)` markers in
  `drafts/ch1_intro.md` or `drafts/ch2_litreview.md`. (The four wiki-side
  markers are reported under M2 and L1.)
- **Dead links from drafts**: clean. All `[[wiki/...]]` and
  `[[wiki/...#anchor]]` references in `drafts/ch1_intro.md` and
  `drafts/ch2_litreview.md` resolve. Anchors verified against actual
  `##` headings on target pages (12 anchored references checked, all
  pass).
- **Contradictions**: no contradictions detected during this pass. T13's
  update to `wiki/concepts/problem-statement.md` is internally
  consistent with `wiki/concepts/research-questions.md` (RQ1–RQ5,
  four-family scope) and with the consolidated `[1]`–`[17]` numbering
  in `wiki/concepts/annotated-bibliography.md`. No `## Revisions` notes
  are missing.

## Tally

- High: 1
- Medium: 2
- Low: 4 (one of which, L4, is informational and recommends no action)

## Next pass

L-W4 (end of Week 4). Expected resolutions by then: T16 lands
`concepts/message-types`, T18 lands `concepts/adversary-model`, T19
lands `concepts/experiment-matrix`, closing the M1 forward-reference
cluster. M2 and L1–L3 are independent of W3–W4 modeling work and can
be addressed at any time.
