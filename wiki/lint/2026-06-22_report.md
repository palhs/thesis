# Wiki lint report — L-W10 (end of Week 10)

- **Date:** 2026-06-22
- **Checkpoint:** L-W10 (Week 10 KPI, after the adversarial experiment battery T51–T56)
- **Scope:** full `wiki/` tree (105 `.md` pages) + `drafts/` (6 chapters) + the
  `TASKS.md`/`results/` cross-checks, against the eight `docs/lint-protocol.md`
  checks.
- **Method:** a deterministic mechanical pass (link-graph build, index/disk
  diff, `TODO` scan, figure-export staleness via git commit times,
  artifact-existence for every `[x]` task) followed by a 32-agent verified
  workflow — seven judgment zones (metric / adversary / algorithm / draft
  contradictions, missing pages, figure-export, structural double-check), every
  candidate finding then re-checked by an independent skeptic that drops
  anything already covered by a `## Revisions` note or unconfirmable from the
  files. Findings below survived that verification; the orchestrator
  re-confirmed all High/Medium items by hand.
- **Report only.** No file was edited. The Linter surfaces; the human triages.

## Summary

The wiki is in good health going into the Week 11 writing crunch. The
Revisions discipline is being applied well: every metric-definition churn from
the T49/T49.1/T71 cycle (PBFT overhead formula, Snowman ε values,
`finality_latency_ms`≡`commit_latency_ms`, `tps`/`goodput` crediting,
`bytes_per_acu` payload-dominance) is correctly confined to dated `## Revisions`
sections with corrected body text, and all were re-verified CSV-exact against
`results/baseline/`. Orphans, dead links, index drift, and citation gaps are
all clean. **Two prior-checkpoint items are resolved** (see Carry-forward).

Two **High** findings are new and worth attention before Chapter 3/4 are
frozen: a knowledge-base page that presents an unbuilt protocol as implemented,
and a load-bearing Casper FFG calibration value where the methodology chapter
disagrees with the code and every result. One new **Medium** is a quantitative
adversary-invariant breach lacking a Revisions note. The remaining items are
Low or already-tracked.

| Severity | Count | New | Already-tracked |
| :-- | :-- | :-- | :-- |
| High | 2 | 2 | 0 |
| Medium | 2 | 1 | 1 |
| Low | 5 | 2 | 3 |

**Update 2026-06-22 (same session):** both High findings (H1, H2) were fixed
after triage — see the per-finding *Resolution* notes below. The Medium and Low
items remain open for the human to triage.

---

## High

### H1 — `dag-based.md` presents Narwhal+Tusk as an implemented simulator target, but it is unbuilt (T38.1 Blocked)

- **Check:** 3 (contradiction). **Status: NEW, untracked.**
- **Locations:** `wiki/algorithms/dag-based.md:32`, `:201`, `:206–219`;
  `src/` (no `narwhal_tusk/` package); `TASKS.md:207` (T38.1 `[!]` Blocked),
  `TASKS.md:192` (T36.2 `[!]` Blocked "pending T38.1").
- **Finding:** The page describes Narwhal+Tusk in the present tense as a live
  build: line 201 "The implementation **is** a simplified Narwhal-like mempool
  with a Tusk-style commit rule"; line 32 "the simulator target **is**
  simplified Narwhal + Tusk"; lines 206–216 list "Knobs exposed to
  experiments:"; lines 218–219 "These feed T37–T40 … and the
  baseline/delay/adversarial experiment batteries in Weeks 8–10." This is
  structurally identical to the *Simulator mapping* sections of the three
  protocols that **are** built (`pbft.md`, `pos.md`, `avalanche.md`). Ground
  truth: `ls src/` shows `pbft pos snowman` but no `narwhal_tusk`/DAG package;
  every `narwhal`/`tusk` reference in `src/` is a placeholder (`float("nan")`
  rows and "deferred Narwhal+Tusk" comments in `src/output/adversary_*.py`).
  T38.1 (implement Narwhal+Tusk) is Blocked. The page carries **no**
  `## Revisions` section and no "not yet implemented / deferred / pending
  T38.1" disclaimer anywhere.
- **Why High:** A core knowledge-base page misrepresents an unbuilt, Blocked
  protocol as a running simulator target with experiment knobs and a Weeks 8–10
  battery — the exact drift `pos.md` was already revised to correct (it splits
  `### Implemented` vs `### Not implemented (deferred)` under a dated Revisions
  note). A Writer drafting the NWT methodology (T36.2) or a reader reproducing
  the work would be misled.
- **Recommendation (human / Researcher — page owner):** Reframe `dag-based.md`
  §Simulator-mapping + §Expected-findings to make the unimplemented status
  explicit, mirroring the `pos.md` pattern: change "The implementation is…" to
  a planned/deferred construction ("The planned implementation (T38.1, blocked)
  will be…"), mark the knob list as a planned interface, qualify or drop the
  "feed … Weeks 8–10" sentence, and add a `## Revisions` entry recording the
  correction.
- **Resolution (2026-06-22, same session):** Fixed. `dag-based.md`
  §Simulator-mapping / §Behaviour-under-adversarial / §Expected-findings /
  §Weaknesses reframed to mark Narwhal+Tusk as **planned (T38.1, Blocked)**,
  mirroring `pos.md`'s "Not implemented (deferred)" pattern; algorithm mechanics
  untouched; `## Revisions` entry added. Independently verified.

### H2 — Casper FFG slot calibration: Ch3 (and the wiki §Calibration table) say `slot_duration=100 ms`/`slots_per_epoch=4`; the code and every result use `1 s`/`2`

- **Check:** 3 (contradiction). **Status: NEW, untracked.**
- **Locations:** DESIGN side — `drafts/ch3_methodology.md:178` (Table 3.2),
  `:225` (§3.3.2 ②), `:228` (§3.3.2 ③), `:426`/`:431` (§3.4.3 coherence rule);
  `wiki/concepts/metric-reconciliation.md:254–265` (§Calibration table).
  AS-RUN side — `src/pos/node.py:77–78` (`slot_duration: float = 1.0`,
  `slots_per_epoch: int = 2`); `wiki/experiments/2026-06-03_scaling-baseline.md:21`
  (`slot_duration = 1 s`, `slots_per_epoch = 2`); `drafts/ch4_results.md:74`
  ("one-second slot and two-slot epoch"), `:252` ("rescaling the slot from one
  second to 1.2 s").
- **Finding:** Ch3 §3.3.2 pins `slots_per_epoch = 4` and `slot_duration =
  100 ms`, copied from the `metric-reconciliation` §Calibration table, which
  states these "keep FFG finality at ~800 ms." But the simulator default is
  `1.0 s`/`2`, every baseline/delay run uses `1 s`/`2`, and Ch4 reports FFG
  finality ≈ **5000 ms** (Table 4.1, §4.2.2) — consistent with two epochs ×
  one-second slot, **not** the design table's ~800 ms. So the methodology
  chapter and the wiki calibration table describe a configuration that did not
  produce the thesis's own results, and Ch3's §3.4.3 derivation
  `slot_duration ≥ 4·E[delay]` reads against the wrong (100 ms) baseline. No
  `## Revisions` note on `metric-reconciliation.md` or `scaling-baseline.md`
  reconciles the divergence (the metric-reconciliation Revisions cover the
  T49.1 items + T71 only; the scaling-baseline Revisions covers the T70
  client-finality fix only).
- **Why High:** This is a load-bearing config value. An examiner reproducing
  from Ch3 would configure FFG at 100 ms/4-slot and get ~800 ms finality, ~6×
  off the reported number; Ch3 and Ch4 contradict each other directly.
- **Recommendation (human decision — Researcher + Writer):** Decide the
  canonical FFG calibration. The data/code ground truth is `slot_duration=1 s`,
  `slots_per_epoch=2` (it produced every Ch4 number). Correct Ch3 §3.3.2 ②③ +
  Table 3.2 to the as-run values and add a `## Revisions` note to
  `metric-reconciliation.md` §Calibration recording that the as-run config
  diverged from the original design table (or, if 100 ms/4 was the intended
  pin, treat the as-run config as the defect — but then the entire results set
  is at the wrong calibration, a much larger issue). Either way the two must be
  reconciled before Ch3 is frozen.
- **Resolution (2026-06-22, same session):** Fixed by reconciling the design
  docs to the as-run `1 s`/`2` (the only resolution that does not invalidate the
  committed dataset; Ch4 §4.2.2 already owns the ~5 s FFG finality). Corrected
  `metric-reconciliation.md` §Calibration table + rationale + coherence
  constraint, `experiment-matrix.md` §3/§5/§9 + RQ3 sweep,
  `experiment-matrix-runs.md` §2 pairing table, and `ch3_methodology.md`
  Table 3.2 + §3.3.2 ②③ + §3.4.3; dated `## Revisions` notes added to all three
  wiki pages. Delay-rescale values (`1200 ms`, `12000 ms` = 4·E[delay])
  unchanged. The rejected alternative (re-run at 100 ms/4) is recorded in the
  metric-reconciliation Revisions note. Independently verified.
- **Robustness follow-up (2026-06-22):** the reconciliation surfaced a deeper
  methodological risk — FFG's absolute finality (≈5 s) is set by the slot
  choice, so the cross-protocol *absolute*-latency comparison is calibration-
  dependent. Addressed by (a) a conditional-framing pass on Ch4 §4.2.2 and (b) a
  FFG slot-duration sensitivity sweep [[experiments/2026-06-22_ffg-slot-sensitivity]]
  proving finality is linear in the slot yet stays above the per-block ≈1 s
  commit across the realistic range (crossover at a sub-realistic 0.2 s) — so the
  epoch-granularity finding is structural, not knob-engineered.

---

## Medium

### M1 — `adversary-model.md` §3 Snowman delay invariant (`accept_time p95 ≤ 5×`) is breached ~10× by T51, with no Revisions note

- **Check:** 3 (contradiction). **Status: NEW, untracked.**
- **Locations:** `wiki/concepts/adversary-model.md:84` (§3 delay-emission
  table, Snowman row); `wiki/concepts/adversary-model.md:290–306` (T51
  Revision); `wiki/experiments/2026-06-14_delayed-voters.md:110`.
- **Finding:** The §3 catalog predicts, for Snowman under delay-emission, the
  checked invariant `accept_time p95 ≤ 5× honest baseline`. T51 measured a
  Snowman finality blow-up of **~54× (n=10), ~49× (n=25)** under the worst
  delay attack (the dose-response table reaches 61.9×) — ~10× past the 5×
  ceiling. The page does carry a `[2026-06-14] T51` Revisions entry, but it
  only notes "Snowman latency inflation" qualitatively and asserts the
  liveness-only classification held; it never records that the specific
  quantitative predicate `p95 ≤ 5×` failed.
- **Why Medium (not Low):** The page's *own convention* records quantitative
  invariant breaches — the T52 Revision explicitly flags its §4 breach, the T53
  Revision its §5 cliff — so the missing §3 note is an inconsistency in the
  page's Revisions discipline, not just an omission. The classification
  (liveness-only, safety holds) is correct and unaffected; only the numeric
  bound is wrong.
- **Recommendation:** Add a `## Revisions` entry on `adversary-model.md` noting
  that T51 breaches the §3 Snowman `accept_time p95 ≤ 5×` invariant (~54×/~49×,
  sequential K-poll over slow responders compounds per-round delay), and either
  revise the 5× ceiling to the measured order of magnitude or reclassify it as
  an unmet calibration placeholder.

### M2 — Ch4 §4.4 Figure 4.17 cites a deleted PDF (`pbft_viewchange_rate_vs_phi.pdf`); the on-disk replacement is an untracked `…_count_…` variant — *already tracked, in-flight*

- **Check:** 8 (figure-export / data-plot). **Status: already-tracked
  (Backlog 2026-06-20, fold into T62); mid-regeneration.**
- **Locations:** `drafts/ch4_results.md:684` ("Source:
  `results/adversary/plots/pbft_viewchange_rate_vs_phi.pdf`");
  `results/adversary/plots/pbft_viewchange_rate_vs_phi.pdf` (git status `D`,
  absent on disk); `results/adversary/plots/pbft_viewchange_count_vs_phi.pdf`
  (git status `??`, present); `src/output/adversary_degradation_plots.py:90`,
  `:142` (still emit the `rate` slug).
- **Finding:** The Figure 4.17 source path points to a PDF the working tree has
  deleted; only the untracked `count`-named variant exists on disk, and the
  plot code still writes the `rate` name — so the draft reference, the committed
  code path, and the on-disk artifact disagree. This is the "rate/count seam"
  surfaced by the 2026-06-20 §4.4 figure review; the §4.4 plot PDFs are being
  actively regenerated (six `M`, one `D`, two `??` under
  `results/adversary/plots/`). Severity reflects build-impact (Figure 4.17 has
  no backing artifact in the tree), not novelty — it is a known item.
- **Recommendation:** Resolve under the T62 §4.4 figure pass: settle on
  `rate` vs `count`, update `ch4_results.md:684` and
  `adversary_degradation_plots.py:90/142` to agree, and commit the regenerated
  PDF. No new Backlog entry needed.

---

## Low

### L1 — `concepts/citation-keys` is an orphan (zero inbound content links) — NEW
- **Check:** 1 (orphan). `grep '[[concepts/citation-keys'` across `wiki/`
  (excluding `index.md`/`log.md`) returns zero; its natural parent
  `concepts/annotated-bibliography.md` links out to many pages but never back to
  it. Reachable via `index.md`, so this is graph hygiene, not lost knowledge.
- **Recommendation:** Add one inbound link from `annotated-bibliography.md` to
  `[[concepts/citation-keys]]`, or accept it as an intentional terminal
  reference.
- *Methodology note:* orphan detection here excludes `index.md` and `log.md` as
  inbound sources (both link every page by design; counting them makes the check
  vacuous). The 9 dated experiment-baseline / lint-report pages with no upstream
  content link are **leaf-by-design** terminal artifacts (reachable from
  index+log) and are **not** flagged.

### L2 — `TASKS.md` T55 artifact path `results/adversarial/` does not exist
- **Check:** 4 (stale status). `TASKS.md:257` (Completed task T55) records
  `_Artifact:_ `results/adversarial/``, but the real directory is
  `results/adversary/` (no "l"); `results/adversarial/` is absent on disk. All
  actual T55 outputs (`adversary_comparison.csv`, `robustness_ranking.csv`, …)
  exist under `results/adversary/`. Cosmetic recorded-path typo that would
  misdirect a reader locating T55 outputs. The typo was flagged in the T54 and
  T55 handoff notes (`wiki/log.md`) as a known spelling slip but never corrected
  in `TASKS.md` and is not in the Backlog.
- **Recommendation:** Correct `TASKS.md:257` to `results/adversary/`.

### L3 — `fork_rate` vs `safety_violation_rate` terminology split — already-tracked (Backlog 2026-06-18)
- **Check:** 3/5 (terminology drift). The three metric-definition pages
  (`metric-reconciliation.md`, `evaluation-metrics.md`, `output-format.md`) and
  Ch3 §3.5 still use `fork_rate` (as both the CSV column and, in older pages,
  the concept name); only the two newest pages
  (`experiments/2026-06-19_{adversarial-degradation,adversary-comparison}.md`)
  use `safety_violation_rate`. This is the **expected mid-rename state** — the
  `fork_rate → safety_violation_rate` rename is an OPEN code/schema Backlog task
  (2026-06-18). `fork_rate = 0` on every honest baseline row, so it is not a
  load-bearing measured value today. Listed once (not per-occurrence).
- **Recommendation:** No new action — folds into the open rename task; update
  all `fork_rate` references together when it lands.

### L4 — `aggregated.csv` "duplicate" casper-ffg n=4 is actually two distinct stake configs — refines Backlog 2026-06-14
- **Check:** 4 (stale status / data integrity). `results/baseline/aggregated.csv`
  rows 2–3 are `casper-ffg-n4-nonuniform` and `casper-ffg-n4-uniform` —
  **distinct** `run_id`s, not a copied row. Their metrics are byte-identical
  because stake distribution does not affect the zero-delay baseline; the pair
  is documented in `output-format.md:300–310` and the scaling-baseline /
  metrics-collection experiment pages. So the Backlog 2026-06-14 "duplicate row"
  framing is better stated as "the nonuniform-stake variant produces metrics
  byte-identical to uniform, adding no information at the baseline." No data
  corruption.
- **Recommendation:** Re-word the Backlog item; no data fix needed.

### L5 — 15 data-plot PDFs under `results/**/plots/` are uncited by any draft figure
- **Check:** 8 (figure-export, informational). Of 28 plot PDFs on disk, 13 are
  cited by a draft Figure `Source:` line. The 15 uncited
  (`decision_rate_vs_n`, `goodput_ci_vs_n`, `latency_vs_n`, `msgs_vs_n`,
  `success_rate_vs_n`, `throughput_vs_n`, `ratio_vs_{f,m}_n{10,25}`,
  `success_vs_f_n{10,25}`, `throughput_vs_f_n{10,25}`, `tradeoff_matrix`) are
  **not orphaned** — the baseline set is backlinked from
  `experiments/2026-06-08_baseline-plots.md`; they simply are not surfaced as
  numbered thesis figures. Authoring choice, not a figure-export defect.
- **Recommendation:** Surface only. Promote any the Writer wants as numbered
  figures (e.g. `tradeoff_matrix`); otherwise leave as analysis artifacts.

---

## Checks that passed clean (no findings)

- **Dead links (7):** zero genuine in live content pages. Every non-resolving
  `[[…]]` is a format-template placeholder (confined to `index.md` header,
  `log.md`, `lint/*_report.md`, `diagrams/index.md` doc block), a spec-mandated
  draft `[[wiki/…]]` citation that strips to a real slug, an intra-page
  `[[#anchor]]` self-link in `pos.md`, or a historical already-fixed reference
  (`[[algorithms/casper-ffg|snowman|narwhal-tusk]]` survive only in `log.md` and
  the L-W4 report, documenting the L-W4.2 repoint; `reproducibility.md` no
  longer references them).
- **Index drift (5):** zero. All 102 on-disk content pages are cataloged in
  `index.md`; every index entry resolves (the 6 `drafts/*` entries + `diagrams/index`
  all exist on disk; the only non-resolving token is the `[[path/to/page]]`
  format example in the header).
- **Citation gaps (6):** zero `TODO(cite)` in `drafts/`; zero real
  `TODO(human-export)` anywhere (the 7 textual hits all *describe* the retired
  marker mechanism).
- **Missing pages (2):** none. Every entity referenced in 3+ pages is
  authoritatively defined exactly once and merely cited elsewhere — AURC
  (`delay-comparison.md:27`), survival-depth, ACU (`metric-reconciliation.md:54`),
  goodput, GST (`synchrony-models.md:13`), Wilson interval
  (`adversarial-degradation-metrics.md:33`), CRN (`experiment-matrix.md:211`),
  `f_max` bracket, slashable-stake, finalization_rate, the Snowman ε bound. The
  healthy defined-once/cited-elsewhere pattern.
- **Metric-definition contradictions (3):** clean. The PBFT overhead formula
  (`2(n²−1)`/`2n−2/n`), Snowman ε values (n=7 `2.1·10⁻¹²`, n=10 `4.9·10⁻¹⁵`,
  n=25 `3.3·10⁻¹¹`), `finality_latency_ms`≡`commit_latency_ms`, `tps`/`goodput`
  crediting, and `bytes_per_acu` payload-dominance each carry corrected body
  text with the superseded form quoted **only** inside a dated `## Revisions`
  section, all re-verified CSV-exact against `results/baseline/`.
- **Adversary catalog (3):** the T52 §4 Snowman withholding cliff and the T53
  §5/§7 equivocate cliffs are properly documented with dated `## Revisions`
  notes on `adversary-model.md` (and `experiment-matrix-runs.md`); the 12-of-18
  coverage count is internally consistent across the catalog pages and its
  catalog-vs-reality lag is the tracked Backlog item 435 ("Watch for T38.1").
  The only uncovered breach is M1 above.
- **Algorithm pages (3 of 4):** `pbft.md`, `pos.md`, `avalanche.md` accurately
  reflect the T70 fidelity fixes (genuine Snowball `argmax(confidence)`, FFG
  double+surround slashing + slashable-stake metric, PBFT `f+1` REPLY
  client-finality), with every still-deferred mechanism (penalty application,
  LMD-GHOST, per-validator attestation delay, validator rotation) marked
  deferred and the residual `pos.md` overclaims already in its `## Revisions`.
  (`dag-based.md` is the exception — H1.)

## Carry-forward from prior checkpoints

- **L-W8 M1 (stale protocol-diagram PDFs) — RESOLVED.** All
  `wiki/diagrams/**/*.md` carrying a `mermaid` block now have a sibling `.pdf`
  newer than its source (the protocol diagrams were re-rendered; commit
  `d8996e4`). No stale Mermaid PDFs remain.
- **L-W8 M2 (data-plot figures uncovered by the figure convention / check 8) —
  RESOLVED.** `docs/lint-protocol.md` check 8 now includes the "Data plots"
  sub-check (8d, figure → experiment-page → `results/**/plots/<slug>.pdf`),
  which this pass exercised. The only data-plot gap it found is the in-flight
  rate/count seam (M2).

---

*Generated for L-W10. Findings only — no wiki content was modified. Triage and
any fixes are the human's to schedule (the two High items in particular should
be resolved before Chapter 3/4 are frozen in Week 11–12).*
