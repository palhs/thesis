# L-W8 — End-of-Week-8 wiki lint report (2026-06-09)

Checkpoint lint pass before the Week 9 delay-experiment phase, after the
Week 8 baseline tasks (T41–T45) landed. Walked the wiki and `drafts/` against
the eight `docs/lint-protocol.md` checks. **Report only — no edits made.**
Triage and any fixes are the human's call.

Scope walked: 4 algorithm pages, ~30 concept pages, 18 source pages, 13
experiment pages, 11 diagram pages, 3 lint reports, 4 chapter drafts +
front matter, `TASKS.md`.

## High

None.

## Medium

- **M1 — Four protocol-diagram PDFs are stale relative to their Mermaid
  source (check 8c).** `wiki/diagrams/protocols/{pbft,casper-ffg,snowman,
  narwhal-tusk}.pdf` were last exported 2026-05-26; their `.md` sources were
  edited 2026-06-05 (`630eb22` "make protocol mermaid diagrams
  Zed-preview-safe"), and that commit changed the Mermaid blocks themselves
  (`(…)` → `[…]`, `—` → `--`, `→` → `=>`), not just prose. The committed PDFs
  therefore render the pre-06-05 glyphs. Chapter 3 §3.3 cites these figures.
  Suggested fix: re-export the four PDFs via the `mmdc` invocation in
  `docs/mermaid-syntax.md` and commit alongside the `.md`, before L-W12
  (which gates submission on check 8).

- **M2 — Data-plot figures have no documented convention or lint coverage.**
  Chapter 4 (T45) introduces six matplotlib data-plot figures
  (`results/baseline/plots/*.pdf`, cited as Figures 4.1–4.6). The figure
  convention in `docs/draft-style.md` §Figures and lint check 8 cover only
  Mermaid diagrams under `wiki/diagrams/<group>/<slug>.pdf`; they say nothing
  about result plots rendered from CSV. Consequence: Ch4's figures are
  unmanaged by the export/lint machinery, and the figure-number ↔ plot-file
  mapping lives only in the T43/T44 experiment pages. Suggested triage: add a
  data-plot figure convention (location, regeneration command, draft
  citation form) to `draft-style.md`, and extend lint check 8 to verify
  `results/**/plots/*.pdf` siblings for `drafts/` figure references, before
  T62 (figure polish) / L-W12.

## Low

- **L1 — `TASKS.md` test-path label drift.** Completed tasks T25
  (`src/tests/`) and T31 (`src/tests/pbft/`) name a `src/tests/` location, but
  the test suites live at top-level `tests/` (12 suites, per the `Makefile`
  `SUITES` list). Cosmetic only — the tests exist and pass; the artifact
  labels are wrong. Suggested fix: relabel to `tests/` / `tests/pbft/`.

- **L2 — Rate-metric interval policy not yet exercised.** T44's
  `results/baseline/aggregated.csv` and Chapter 4 report `success_rate` (1.0)
  and `fork_rate` (0.0) as point values with zero-width CIs, whereas Ch3 §3.5
  prescribes a 95% Wilson binomial interval for rate metrics. This is
  defensible at the honest baseline — the outcomes are deterministic across
  all 20 seeds, not a stochastic sample — but the Wilson rule is unexercised.
  Confirm it is wired in once T48 (delay) / T54 (adversarial) make these
  metrics genuinely stochastic, so §3.5's contract is honoured where it bites.

- **L3 — Directory-target wikilink.** `wiki/concepts/annotated-bibliography.md`
  carries `[[sources/|source pages]]`, which resolves to the `sources/`
  directory rather than a specific page. Minor; consider pointing at a
  concrete page or the index `## Sources` section.

## Clean (checked, no findings)

- **Orphans (check 1):** clean. Nine experiment/lint pages are reachable only
  from `wiki/index.md` — the established, accepted pattern for catalog leaf
  pages (every prior experiment page is index-linked the same way); not a
  regression. The two new T43/T44 pages are additionally cross-linked
  (`baseline-plots` → `baseline-cis`) and cited from `drafts/ch4_results.md`.
- **Missing pages (check 2):** clean. No entity is referenced across 3+ pages
  without its own page; the recurring entities all resolve.
- **Contradictions (check 3):** clean. The Student-t-vs-Gaussian CI choice and
  the post-T70 `commit_latency_ms` comparable-column rule are both recorded as
  dated `## Revisions` (output-format §13) and disclosed in Ch4 prose — no
  silent overwrite.
- **Stale status (check 4):** clean. Every completed-task **file** artifact is
  present. (The mechanical scan's apparent hits are `file.md:LINE` references,
  the deliberately renamed-from `src/logging`→`src/event_log`, gitignored
  runtime CSVs, and wikilink targets — none a genuinely missing artifact.)
- **Index drift (check 5):** clean. Every catalogued page is on disk; every
  on-disk page (incl. the two new experiment pages + `drafts/ch4_results`) is
  in the index.
- **Citation gaps (check 6):** clean. No `TODO(cite)` in `drafts/`.
- **Dead links (check 7):** clean. No wikilink points to a non-existent page
  (after excluding documentation placeholders and historical mentions in
  `log.md` / earlier lint reports).
- **Figure-export markers (check 8a):** clean. The five `TODO(human-export)`
  string occurrences (`log.md`, `diagrams/index.md`) are descriptive
  references to the retired marker pattern, not live markers.

## Summary

No High findings. Two Medium items both concern figure handling and are
naturally owned by the figure-polish phase (T62 / L-W12): re-export the stale
protocol diagram PDFs (M1) and extend the figure convention + lint to cover
data plots (M2). The three Low items are cosmetic or forward-looking. The
Week-8 baseline knowledge base is internally consistent and ready for the
Week-9 delay experiments.
